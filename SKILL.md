---
name: capybara-child-game-builder
description: Build or adapt the Capybara Bubble Tea Catcher for a child by wiring accounts, character art, animation, helper skills, and effects; do not use for unrelated game genres.
---

# 水豚奶茶儿童游戏搭建

## 作用范围

这个 Skill 只服务于当前这一款“水豚奶茶接物游戏”（项目目录通常为
`capybara-bubble-tea-catcher`）。它帮助把一个或多个孩子的昵称、登录、角色素材、
角色动作、召唤技能和技能特效接入这款游戏。不要把它扩展成跑酷、躲障碍、卡牌或
其他游戏的通用框架；遇到其他游戏，应停下来说明不属于本 Skill 的范围。

如果用户的描述还没有明确要求做什么，只讨论方案，不修改文件。只有用户明确要求
接入、替换、修改或测试时才执行相应动作。

## 重要边界

- 默认只在本地项目工作。只有用户明确说“部署、上线、同步到服务器”时，才进入线上流程。
- 未获明确授权时，不连接、不修改、不重启线上服务，也不把本地内容自动推送到线上。
- 角色图片、昵称、密码和分数是项目数据；不得把本地运行数据、私钥或服务器凭据打包进 Skill。
- 不替用户擅自决定孩子的技能规则。技能没有确定时，可以先做角色素材和动作外壳，保留“技能待设计”，不要自行发明会改变得分、扣血或关卡的规则。
- 保留现有接物玩法、登录和排行榜，除非用户明确要求调整它们。

## 开始前先读取的资料

需要实现或改动角色时，先读：

- [references/child-role-form.md](references/child-role-form.md)：向用户收集每个孩子的最小资料，并把“角色动作、技能逻辑、技能特效”分开。
- [references/implementation-contract.md](references/implementation-contract.md)：当前游戏的文件、状态机、得分和召唤约束。

如果安装环境中有 `capybara-helper-character` Skill，处理角色图片时一并使用它；否则按
`implementation-contract.md` 中的素材要求完成同等处理。

## 标准工作流

### 1. 识别当前项目和改动范围

先确认工作目录确实是水豚奶茶接物游戏，并查看 `index.html`、`script.js`、
`style.css`、`server.js`、`assets/` 和 `data/`。区分代码、素材和运行时数据：
运行时账号/排行榜数据不能当作 Skill 配置提交。

根据用户提供的人数建立 1～4 个孩子账号。一个孩子时默认使用第一个账号和第一个
助手位；不要因为人数少就重排已有助手 ID。四个位置的顺序是稳定契约。

### 2. 先整理成孩子卡片，再动代码

每个孩子至少要形成一张卡片：

```text
账号位：1～4
昵称：显示在登录、游戏和排行榜中的名字
密码：登录口令；是否在登录页明示由用户决定
角色素材：本地图片路径或明确的生成要求
技能名称：孩子给出的名称
技能效果：实际改变什么、持续多久、作用对象是什么
技能特效：颜色、粒子、光环、冰冻、清场等视觉表现
动作：进场、施法、离场分别怎么动
```

如果用户只给了图片，没有给技能规则，就只完成透明素材、角色显示和基础动作，
并明确列出还缺什么；不要把“看起来像某种角色”当成已授权的技能效果。

### 3. 处理角色素材

- 保留孩子提供的脸部、服装、发色和明显特征；只清理背景、边缘杂色和不需要的水印。
- 游戏素材优先输出为透明背景 RGBA PNG；建议角色主体完整、留少量边缘安全区，目标约 512×512，必要时按比例缩放。
- 不覆盖原始图片，输出到游戏的 `assets/`，并使用稳定、无个人信息的文件名，例如 `helper-1.png`。
- 检查图片确实有 alpha 通道、主体没有被裁掉、浏览器能以 `image/png` 访问。
- 角色是单张立绘时，用 CSS/JS 的位移、缩放、淡入淡出、轻微摆动和光效做动画；不要假装单张图已经是逐帧动画。若用户提供逐帧图，再按帧动画接入。

### 4. 把角色动作和技能拆开实现

每个助手都要能独立说明三层内容：

1. **角色动作**：从画面外进场、停留、施法姿态、离场。当前项目要求进场动作在 2 秒内完整结束，允许加速但不能只完成一半；进场期间全局暂停下落和碰撞，进场结束后恢复。
2. **技能逻辑**：它对得分、扣血、道具、速度或当前场景做什么；是否一次性、持续时间、是否可叠加、结束后如何恢复。
3. **技能特效**：玩家看到什么，例如光环、冰冻、清场、星星、闪光和文字提示。特效结束必须与技能状态同步，不能留下失效的永久状态。

把技能配置放进现有的 `HELPERS` 数据和现有召唤状态机中，不为每个助手复制一套
独立循环。保持助手 ID 和召唤顺序稳定；具体字段和生命周期见实现契约。

### 5. 本地验证

至少完成以下验证，并在结果中报告：

- `node --check script.js` 和 `node --check server.js` 通过。
- 本地服务可以启动，登录页显示正确昵称；若用户要求明示密码，所选账号的密码提示同步更新。
- 角色资源可加载，移动端和桌面端不遮挡控制区。
- 正常积累召唤值后测试一次召唤；同时检查测试入口不会遗留在正式页面。
- 观察完整的“进场（不超过 2 秒）→施法→效果→离场”链路；进场时全局暂停，结束后恢复。
- 检查加分、扣分、血量、排行榜和重开逻辑没有被角色接入破坏。

### 6. 线上流程仅在明确授权后执行

用户明确要求上线后，先备份将要替换的游戏文件和运行数据，再只重启游戏自己的
服务，最后检查游戏域名、Nginx 配置、健康状态，以及同服务器原有站点仍能返回正常
状态。若发现原项目受影响，停止继续发布并优先恢复备份。用户只要求本地时，最终
回答必须明确写“未部署线上”。

## 最终交付应说明

简洁列出：改了哪些文件、加入了哪些素材和角色、每个技能的实际规则、本地访问地址、
做过哪些验证，以及是否部署线上。不要只说“完成”，也不要把未验证的效果说成已完成。
